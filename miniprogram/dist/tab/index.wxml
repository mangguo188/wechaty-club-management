<view class="wux-class {{ classes.wrap }}" bindtap="onTap">
    <view class="{{ classes.title }}" wx:if="{{ title }}"><text>{{title}}</text></view>
    <block wx:else>
        <slot></slot>
    </block>
    <view class="{{ classes.bar }}" wx:if="{{ current }}"></view>
</view>