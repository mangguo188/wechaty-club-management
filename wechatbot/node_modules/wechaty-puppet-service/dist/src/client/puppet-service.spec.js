#!/usr/bin/env ts-node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tstest_1 = require("tstest");
const puppet_service_1 = require("./puppet-service");
tstest_1.test('version()', async (t) => {
    const puppet = new puppet_service_1.PuppetService({
        token: 'test',
    });
    t.ok(puppet.version());
});
/**
 * Huan(202003):
 *  need to setup a test server to provide test token for Puppet Service
 */
tstest_1.test.skip('PuppetService restart without problem', async (t) => {
    const puppet = new puppet_service_1.PuppetService();
    try {
        for (let i = 0; i < 3; i++) {
            await puppet.start();
            await puppet.stop();
            t.pass('start/stop-ed at #' + i);
        }
        t.pass('PuppetService() start/restart successed.');
    }
    catch (e) {
        t.fail(e);
    }
});
//# sourceMappingURL=puppet-service.spec.js.map