#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const puppet_config_1 = require("../src/puppet-config");
async function main() {
    const puppetNameList = Object.keys(puppet_config_1.PUPPET_DEPENDENCIES);
    const publicNameList = puppetNameList.filter(name => /^[^@]/.test(name));
    const urlPath = publicNameList.join('-vs-');
    const url = 'https://www.npmtrends.com/' + urlPath;
    console.info(url);
}
main()
    .catch(console.error);
//# sourceMappingURL=providers-trends.js.map